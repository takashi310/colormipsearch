package org.janelia.colormipsearch.cmd;

public enum ExportedResultType {
    // color depth search matches
    PER_MASK_CDS_MATCHES,
    PER_TARGET_CDS_MATCHES,
    // PPP matches
    PPP_MATCHES,
    // MIPs
    EM_MIPS,
    LM_MIPS
}
